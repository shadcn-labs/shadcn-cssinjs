---
title: Popover
description: Displays rich content in a portal, triggered by a button.
links:
  doc: https://base-ui.com/react/components/popover
  api: https://base-ui.com/react/components/popover#api-reference
---

import PopoverDemo from "@/examples/popover-demo";

<ComponentPreview name="popover-demo">
  <PopoverDemo />
</ComponentPreview>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
pnpm dlx shadcn-cssinjs@latest add popover
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
pnpm add @base-ui/react @stylexjs/stylex
```

<Step>
  Make sure [StyleX is configured](/docs/installation) and the design tokens are
  installed.
</Step>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="popover" title="popover.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover/popover";
```

```tsx
<Popover>
  <PopoverTrigger>Open</PopoverTrigger>
  <PopoverContent>Place content for the popover here.</PopoverContent>
</Popover>
```

## Composition

Use the following composition to build a `Popover`:

```txt
Popover
├── PopoverTrigger
└── PopoverContent
```

## Examples

import PopoverBasic from "@/examples/popover-basic";
import PopoverAlignments from "@/examples/popover-alignments";

### Basic

Use `PopoverHeader`, `PopoverTitle`, and `PopoverDescription` to structure the
content.

<ComponentPreview name="popover-basic">
  <PopoverBasic />
</ComponentPreview>

### Align

Use the `align` prop on `PopoverContent` to align the popover.

<ComponentPreview name="popover-alignments">
  <PopoverAlignments />
</ComponentPreview>

### With Form

Place a small form inside the popover.

<ComponentPreview name="popover-demo">
  <PopoverDemo />
</ComponentPreview>

import PopoverRtl from "@/examples/popover-rtl";

## RTL

To enable right-to-left support, see the [RTL guide](/docs/rtl).

<ComponentPreview name="popover-rtl" direction="rtl">
  <PopoverRtl />
</ComponentPreview>

## API Reference

See the [Base UI](https://base-ui.com/react/components/popover#api-reference)
documentation for the full API.
