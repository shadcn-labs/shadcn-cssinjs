---
title: Input
description: Displays a form input field or a component that looks like an input field.
---

import InputDemo from "@/examples/input-demo";

<ComponentPreview name="input-demo">
  <InputDemo />
</ComponentPreview>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://shadcn-cssinjs.vercel.app/r/input.json
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Make sure [StyleX is configured](/docs/installation) and the design tokens are installed.</Step>

<Step>Copy the component source into `components/ui/input/input.tsx` and its styles into `components/ui/input/input.stylex.ts`.</Step>

<ComponentSource name="input" title="input.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { Input } from "@/components/ui/input/input";

<Input type="email" placeholder="Email" />
```
