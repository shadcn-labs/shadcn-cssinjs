---
title: Sheet
description: Extends the Dialog component to display content that complements the main content of the screen.
links:
  doc: https://base-ui.com/react/components/dialog
---

import SheetDemo from "@/examples/sheet-demo";
import SheetSide from "@/examples/sheet-side";

<ComponentPreview name="sheet-demo">
  <SheetDemo />
</ComponentPreview>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://shadcn-cssinjs.vercel.app/r/sheet.json
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
pnpm add @base-ui/react @stylexjs/stylex
```

<Step>
  Make sure [StyleX is configured](/docs/installation) and the design tokens are
  installed.
</Step>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="sheet" title="sheet.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet/sheet";
```

```tsx
<Sheet>
  <SheetTrigger>Open</SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>Are you absolutely sure?</SheetTitle>
      <SheetDescription>This action cannot be undone.</SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>
```

## Composition

Use the following composition to build a `Sheet`:

```txt
Sheet
├── SheetTrigger
└── SheetContent
    ├── SheetHeader
    │   ├── SheetTitle
    │   └── SheetDescription
    └── SheetFooter
        └── SheetClose
```

## Examples

### Side

Use the `side` prop on `SheetContent` to set which edge the sheet slides from:
`top`, `right`, `bottom`, or `left`.

<ComponentPreview name="sheet-side">
  <SheetSide />
</ComponentPreview>

## RTL

To enable right-to-left support, see the [RTL guide](/docs/rtl).

<ComponentPreview direction="rtl">
  <SheetDemo />
</ComponentPreview>
