---
title: Scroll Fade
description: Mask overflowing content with a subtle block or inline edge fade.
---

```tsx
import { stylex, utilityStyles } from "@/components/ui/utils/stylex-utils";

<div {...stylex.props(utilityStyles.scrollFadeBlock)}>{children}</div>;
```

Use `scrollFadeInline` for horizontal scrollers.
